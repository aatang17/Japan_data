
# EDINET raw capture archive (M2) — large, immutable, never committed
equity/data/

.projects/cache
.projects/vault
.projects/state.test.json
.projects/state.local.test.json
.env
.env.*
!.env.example

# M1 prototype artifacts (raw filings + outputs) — reproducible, not source
equity/m1/
